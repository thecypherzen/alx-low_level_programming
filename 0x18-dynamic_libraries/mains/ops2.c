
int add(int a, int b)
{
	return ((a * a) + (b * b));
}

int sub(int a, int b)
{
	return ((a * a) - (b * b));
}

int mul(int a, int b)
{
	return ((a - b) * (b - a));
}

int divv(int a, int b)
{
	return ((a * a) / (b * b));
}

int mod(int a, int b)
{
	return ((a * a) % (b * b));
}
