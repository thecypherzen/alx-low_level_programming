#include "main.h"
#include <stdio.h>
#include <math.h>

/**
 * main - Entry point
 * Description goes here
 * Return: 0 always
 */
int main(void)
{
/*	int n;
	unsigned int m, p;

	n = (pow(2, 32))/2 - 1;
	m = pow(2, 32) - 1;
	p = (pow(2, 16) - 1) * (pow(2, 16) - 1);


	printf("n(int):\t%d\n",n);
	printf("m(u.int):\t%u\n", m)
	unsigned long n;

	n = 9223372036854775807; */
	int n;

	n = 17;
	printf("sqrt(%d) :%d\n", n, (int)sqrt(17));
	return (0);
}
